apiVersion: v1
kind: Service
metadata:
  name: discourse-tradewars-api
  namespace: discourse
spec:
  type: ClusterIP
  selector:
    app: discourse-tradewars-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
